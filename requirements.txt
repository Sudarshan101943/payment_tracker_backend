fastapi==0.111.0
uvicorn[standard]==0.30.1
pandas==2.1.3        # prebuilt wheel available, avoids compilation issues
openpyxl==3.1.5      # for reading Excel tenant data
python-multipart==0.0.6  # if you need file uploads in future
aiofiles==23.2.1     # for async file handling with FastAPI
requests==2.31.0     # if any HTTP requests are needed
